<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="mx.org.inegi.movil_ikqss">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Principal"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Envios"
            android:label="@string/envios"
            android:theme="@style/AppTheme.NoActionBar.Delete" />
        <activity
            android:name=".Descargas"
            android:label="@string/descargas"
            android:theme="@style/AppTheme.NoActionBar.Delete" />
        <activity
            android:name=".Levantamiento"
            android:label="@string/descargas"
            android:theme="@style/AppTheme.NoActionBar.Delete" />
        <activity
            android:name=".Reasignaciones"
            android:label="@string/reasignacion"
            android:theme="@style/AppTheme.NoActionBar.Delete" />
        <activity
            android:name=".Notas"
            android:label="@string/notas"
            android:theme="@style/AppTheme.NoActionBar.Delete" />
    </application>

</manifest>